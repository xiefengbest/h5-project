# 当前 `web-anyselected/index.html` 与参考站点差异说明

**参考站点**：[https://n1.anyselected.com/](https://n1.anyselected.com/)（以下称「参考站点」）。  
**本地对照**：`pages/web-anyselected/index.html`（静态单页 Demo，高保真原型用）。

> 说明：线上页面可能随版本迭代变化；下表以当前可观察内容与本地仓库为准。若线上增加了未在下列摘录中的模块，以实际线上为准补一行差异即可。

---

## 1. 品牌与页脚文案

| 维度 | 本地 `web-anyselected/index.html` | 参考站 [n1.anyselected.com](https://n1.anyselected.com/) |
|------|-------------------------|----------------------------------------------------------|
| 站点/品牌展示 | **AnySelected**，页脚链为 **AnySelected.com** | 页脚为 **© 2026 News**（品牌呈现为 **News**） |
| 页面标题习惯 | 多为 `{页面内容} · AnySelected` | 摘录显示页面标题为 **News**（与本地命名习惯不同） |

---

## 2. 内容与数据源

| 维度 | 本地 HTML | 线上站点 |
|------|-----------|----------|
| 资讯数据 | 内嵌 **5 条**示例稿，固定 ID 与配图 | **实时/海量**资讯列表，多来源（如 FOX、BBC、Yahoo、CBS 等）与多日期 |
| 详情与推荐 | 本地逻辑 + 固定 `NEWS` 数组 | 应由 **后端或 CMS** 驱动；列表与详情与本地 Demo **不一致** |
| 地域/版本 | 无「国家/地区」选择器 | 摘录显示 **United States**（地区或版本选择类入口） |

---

## 3. 分类（频道）

| 维度 | 本地 HTML | 线上站点（摘录） |
|------|-----------|------------------|
| 分类集合 | **全部、交通、环境、航空、商业、科学**（偏主题 Demo） | **All、news、entertainment、finance、sports、health、celebrities、technology、lifestyle** |
| 语义 | 与真实新闻站点频道 **不完全同一套** | 更贴近综合新闻站 **频道划分** |

本地若要对齐线上，需 **替换分类 ID、文案与筛选项**，并保证 URL/路由策略与线上一致（由产品定义）。

---

## 4. 语言与界面

| 维度 | 本地 HTML | 线上站点（摘录） |
|------|-----------|------------------|
| 多语言 | **显式语言切换**（多语种列表 + 持久化 + URL `lang` 规则） | 公开摘录中 **未体现**与本地同构的语言抽屉；是否有多语言以线上实际为准 |
| 默认语言 | 本地约定 **默认英文** | 线上摘录内容为 **英文资讯**，无法从摘录单独推断默认语言策略是否与本地一致 |

---

## 5. 功能模块（本地有 / 线上摘录未体现）

| 模块 | 本地 HTML | 线上（当前摘录） |
|------|-----------|------------------|
| 顶栏 | **不在页内实现**（文档约定由 WebView 原生提供返回与标题） | 摘录未描述浏览器内顶栏，可能由站点自身布局或 App 壳提供 |
| 搜索 | **标题搜索** + 结果面板 | 摘录中 **未出现**与本地相同的搜索框行为 |
| 静态说明页 | **无障碍、关于、隐私、服务条款** 内嵌模板，`?page=` 切换 | 页脚有 **Accessibility / About / Privacy / Terms** 链接，**实现方式**可能与本地单页模板不同（独立路由或 SSR） |

---

## 6. 技术形态（高层）

| 维度 | 本地 HTML | 线上站点（推断） |
|------|-----------|------------------|
| 交付形态 | **单文件** HTML + 内联样式与脚本 + 内嵌数据 | 通常为 **独立部署的前端应用** + **接口数据**（与 Demo 架构不同） |
| SEO / 链接 | 本地有完整 query 约定（`id`、`page`、`lang`、`cat` 等） | 线上 URL 结构 **未必**与本地 Demo 一一相同，需单独对齐 |

---

## 7. 小结（给产品/研发）

- **一致或相近**：整体是「资讯列表 + 说明页入口」类产品；页脚四项法律/说明入口在语义上对应。  
- **主要差异**：**品牌名**（AnySelected / News）、**版权行**、**分类体系**、**数据规模与来源**、**地区选择**；本地 Demo **多语言 + 搜索 + 固定 query 状态机** 在公开摘录中 **未全部验证**线上是否同等实现。  
- **对照参考站时**：应以 [n1.anyselected.com](https://n1.anyselected.com/) 的**分类、品牌、地区、接口与路由**为准，将 `web-anyselected/index.html` 视为**交互与逻辑的参考原型**，而非参考站镜像。

---

*文档用途：记录 Demo 与现网差异，避免误以为本地 HTML 即生产一致。*
